import { FloatingNavPage } from "@/components/floating-nav-page";

// Playground /5: /4's bottom-crop image page plus a floating glass pill nav, a
// section overlay, and a (non-functional) "with / without mutual" toggle.
export default function Page() {
  return <FloatingNavPage />;
}
