import { FloatingNavPage } from "@/components/floating-nav-page";

// Playground /5: identical to /4 (bottom-crop image + "Ben Piggin" text) plus a
// floating glass pill nav, a section overlay, and a (non-functional)
// "with / without mutual" toggle.
export default function Page() {
  return <FloatingNavPage />;
}
